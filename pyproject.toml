[project]
name = "netcore-cpaas-mcp"
version = "0.1.0"
description = "Netcore WhatsApp API MCP Tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp-server",
    "mcp[cli]>=1.26.0",
    "python-dotenv",
    "requests",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
